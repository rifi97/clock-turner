# 시계 돌리기

`index.html`을 브라우저에서 열거나 아래 명령으로 로컬 서버를 시작합니다.

```sh
python3 -m http.server 4173
```

시작·종료 시각을 설정하고 `감기는 시간`에 원하는 재생 시간을 초 단위로 입력한 뒤 **시계 돌리기**를 누릅니다. 버튼을 한 번 더 누르면 재생이 멈춥니다.

승인된 Visual Ralph 기준 이미지와 검증 산출물은 `.omx/artifacts/visual-ralph/stopwatch-20260714/`에 있습니다.
